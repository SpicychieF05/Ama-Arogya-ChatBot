# FastAPI and Web Framework (pin to versions compatible with Rasa 3.6 / Pydantic v1)
fastapi==0.95.2
uvicorn[standard]==0.24.0
pydantic==1.10.9
python-multipart==0.0.6

# HTTP and Request Handling
requests==2.32.5
httpx==0.25.0

# Database
sqlalchemy==1.4.54

# Configuration
python-dotenv==1.0.0

# Rasa and NLP
rasa==3.6.21
rasa-sdk==3.6.2

# Additional ML dependencies for Rasa (compatible pins)
# Rasa 3.6 requires scikit-learn <1.2
scikit-learn==1.1.3
# Let Rasa control TensorFlow version to avoid conflicts across platforms
# (Rasa installs the appropriate TF extra). If you need CPU-only:
# tensorflow-cpu==2.12.1
# Spacy 3.5.x aligns better with that toolchain
spacy==3.5.4

# Language support (optional)
# transformers can be heavy and conflict with certain TF/Numpy pins; enable only if needed
# transformers==4.35.2

# Development and Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Production deployment
gunicorn>=20.1.0